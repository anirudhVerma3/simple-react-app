# Example environment variables for the React App (prefixed with REACT_APP_)
REACT_APP_API_BASE_URL=https://api.example.com/v1
REACT_APP_TITLE="My Awesome CI/CD React App"

# Variables used by the build process itself (not directly by React app)
# DOCKER_REGISTRY_USER=your_docker_user
# DOCKER_REGISTRY_PASSWORD=your_docker_password